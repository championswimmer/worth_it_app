import 'package:flutter/material.dart';

class HomeWishlistPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Wishlist'),
      ),
    );
  }
}
